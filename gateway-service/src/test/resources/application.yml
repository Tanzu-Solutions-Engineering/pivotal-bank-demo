hystrix:
  metrics:
    enabled: false
spring:
  sleuth:
    stream:
      enabled: false
  cloud:
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false
