pivotal:
  quotesService:
    name: quotes-service
  accountsService:
    name: accounts-service
vcap:
  services:
    es:
      credentials:
        cluster-name: elasticsearch
        host: 10.193.152.236
        port: 30141
hystrix:
  metrics:
    enabled: false
spring:
  sleuth:
    stream:
      enabled: false
  cloud:
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false