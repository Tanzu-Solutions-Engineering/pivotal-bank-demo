pivotal:
   quotesService:
     name: quotes-service
   accountsService:
     name: accounts-service
hystrix:
  metrics:
    enabled: false
spring:
  sleuth:
    stream:
      enabled: false
  cloud:
    kubernetes:
      enabled: false
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false