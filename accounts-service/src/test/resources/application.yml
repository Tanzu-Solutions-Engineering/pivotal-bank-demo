spring:
  sleuth:
    stream:
      enabled: false
  cloud:
    kubernetes:
      enabled: false
    discovery:
      enabled: false
    service-registry:
      auto-registration:
        enabled: false